fastapi==0.111.0
uvicorn[standard]==0.29.0
# SQLAlchemy 2.0.30 crashes on Python 3.14 (FastIntFlag/__firstlineno__ collision).
# 2.0.36+ ships a native cp314 wheel and the fix. Pin to >=2.0.36, <3.
sqlalchemy>=2.0.36,<3
psycopg2-binary==2.9.12
alembic>=1.13.1
# pydantic 2.7.1 / pydantic-settings 2.2.1 require pydantic-core built with
# pyo3 <=0.21 which only supports Python <=3.12. Bumped to >=2.7.1 so pip
# resolves the latest patch that ships a cp314 wheel (no Rust toolchain needed).
pydantic>=2.7.1,<3
pydantic-settings>=2.2.1,<3
python-dotenv>=1.0.1
email-validator==2.1.1
